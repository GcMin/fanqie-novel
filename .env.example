GITHUB_REPOSITORY=GcMin/fanqie-novel
GITHUB_BRANCH=main
FANQIE_BOOK_ID=7684310849036504126
# 校验时须与后台作品名称完全一致，改名后同步修改
FANQIE_BOOK_TITLE=零点异常工单
# 每两小时扫描 GitHub ready 目录；不按 commit 判定新增章节
PUBLISH_INTERVAL_SECONDS=7200
# 登录心跳独立运行，不触发章节推送
HEARTBEAT_INTERVAL_SECONDS=300
# 0 = 每轮按章号处理全部新增 ready 章节；审核中仍等待核验
MAX_CHAPTERS_PER_RUN=0
# 必须明确开启才会点击公开发布；check 命令始终只在本地检查
PUBLISH_ENABLED=false
